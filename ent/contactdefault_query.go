// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/cateiru/cateiru.com/ent/contactdefault"
	"github.com/cateiru/cateiru.com/ent/predicate"
)

// ContactDefaultQuery is the builder for querying ContactDefault entities.
type ContactDefaultQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.ContactDefault
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ContactDefaultQuery builder.
func (cdq *ContactDefaultQuery) Where(ps ...predicate.ContactDefault) *ContactDefaultQuery {
	cdq.predicates = append(cdq.predicates, ps...)
	return cdq
}

// Limit the number of records to be returned by this query.
func (cdq *ContactDefaultQuery) Limit(limit int) *ContactDefaultQuery {
	cdq.ctx.Limit = &limit
	return cdq
}

// Offset to start from.
func (cdq *ContactDefaultQuery) Offset(offset int) *ContactDefaultQuery {
	cdq.ctx.Offset = &offset
	return cdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cdq *ContactDefaultQuery) Unique(unique bool) *ContactDefaultQuery {
	cdq.ctx.Unique = &unique
	return cdq
}

// Order specifies how the records should be ordered.
func (cdq *ContactDefaultQuery) Order(o ...OrderFunc) *ContactDefaultQuery {
	cdq.order = append(cdq.order, o...)
	return cdq
}

// First returns the first ContactDefault entity from the query.
// Returns a *NotFoundError when no ContactDefault was found.
func (cdq *ContactDefaultQuery) First(ctx context.Context) (*ContactDefault, error) {
	nodes, err := cdq.Limit(1).All(setContextOp(ctx, cdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{contactdefault.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cdq *ContactDefaultQuery) FirstX(ctx context.Context) *ContactDefault {
	node, err := cdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ContactDefault ID from the query.
// Returns a *NotFoundError when no ContactDefault ID was found.
func (cdq *ContactDefaultQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = cdq.Limit(1).IDs(setContextOp(ctx, cdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{contactdefault.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cdq *ContactDefaultQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := cdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ContactDefault entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ContactDefault entity is found.
// Returns a *NotFoundError when no ContactDefault entities are found.
func (cdq *ContactDefaultQuery) Only(ctx context.Context) (*ContactDefault, error) {
	nodes, err := cdq.Limit(2).All(setContextOp(ctx, cdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{contactdefault.Label}
	default:
		return nil, &NotSingularError{contactdefault.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cdq *ContactDefaultQuery) OnlyX(ctx context.Context) *ContactDefault {
	node, err := cdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ContactDefault ID in the query.
// Returns a *NotSingularError when more than one ContactDefault ID is found.
// Returns a *NotFoundError when no entities are found.
func (cdq *ContactDefaultQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = cdq.Limit(2).IDs(setContextOp(ctx, cdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{contactdefault.Label}
	default:
		err = &NotSingularError{contactdefault.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cdq *ContactDefaultQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := cdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ContactDefaults.
func (cdq *ContactDefaultQuery) All(ctx context.Context) ([]*ContactDefault, error) {
	ctx = setContextOp(ctx, cdq.ctx, "All")
	if err := cdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ContactDefault, *ContactDefaultQuery]()
	return withInterceptors[[]*ContactDefault](ctx, cdq, qr, cdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cdq *ContactDefaultQuery) AllX(ctx context.Context) []*ContactDefault {
	nodes, err := cdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ContactDefault IDs.
func (cdq *ContactDefaultQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if cdq.ctx.Unique == nil && cdq.path != nil {
		cdq.Unique(true)
	}
	ctx = setContextOp(ctx, cdq.ctx, "IDs")
	if err = cdq.Select(contactdefault.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cdq *ContactDefaultQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := cdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cdq *ContactDefaultQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cdq.ctx, "Count")
	if err := cdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cdq, querierCount[*ContactDefaultQuery](), cdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cdq *ContactDefaultQuery) CountX(ctx context.Context) int {
	count, err := cdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cdq *ContactDefaultQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cdq.ctx, "Exist")
	switch _, err := cdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cdq *ContactDefaultQuery) ExistX(ctx context.Context) bool {
	exist, err := cdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ContactDefaultQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cdq *ContactDefaultQuery) Clone() *ContactDefaultQuery {
	if cdq == nil {
		return nil
	}
	return &ContactDefaultQuery{
		config:     cdq.config,
		ctx:        cdq.ctx.Clone(),
		order:      append([]OrderFunc{}, cdq.order...),
		inters:     append([]Interceptor{}, cdq.inters...),
		predicates: append([]predicate.ContactDefault{}, cdq.predicates...),
		// clone intermediate query.
		sql:  cdq.sql.Clone(),
		path: cdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ContactDefault.Query().
//		GroupBy(contactdefault.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cdq *ContactDefaultQuery) GroupBy(field string, fields ...string) *ContactDefaultGroupBy {
	cdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ContactDefaultGroupBy{build: cdq}
	grbuild.flds = &cdq.ctx.Fields
	grbuild.label = contactdefault.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ContactDefault.Query().
//		Select(contactdefault.FieldName).
//		Scan(ctx, &v)
func (cdq *ContactDefaultQuery) Select(fields ...string) *ContactDefaultSelect {
	cdq.ctx.Fields = append(cdq.ctx.Fields, fields...)
	sbuild := &ContactDefaultSelect{ContactDefaultQuery: cdq}
	sbuild.label = contactdefault.Label
	sbuild.flds, sbuild.scan = &cdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ContactDefaultSelect configured with the given aggregations.
func (cdq *ContactDefaultQuery) Aggregate(fns ...AggregateFunc) *ContactDefaultSelect {
	return cdq.Select().Aggregate(fns...)
}

func (cdq *ContactDefaultQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cdq); err != nil {
				return err
			}
		}
	}
	for _, f := range cdq.ctx.Fields {
		if !contactdefault.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cdq.path != nil {
		prev, err := cdq.path(ctx)
		if err != nil {
			return err
		}
		cdq.sql = prev
	}
	return nil
}

func (cdq *ContactDefaultQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ContactDefault, error) {
	var (
		nodes = []*ContactDefault{}
		_spec = cdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ContactDefault).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ContactDefault{config: cdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cdq *ContactDefaultQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cdq.querySpec()
	_spec.Node.Columns = cdq.ctx.Fields
	if len(cdq.ctx.Fields) > 0 {
		_spec.Unique = cdq.ctx.Unique != nil && *cdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cdq.driver, _spec)
}

func (cdq *ContactDefaultQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(contactdefault.Table, contactdefault.Columns, sqlgraph.NewFieldSpec(contactdefault.FieldID, field.TypeUint32))
	_spec.From = cdq.sql
	if unique := cdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cdq.path != nil {
		_spec.Unique = true
	}
	if fields := cdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, contactdefault.FieldID)
		for i := range fields {
			if fields[i] != contactdefault.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cdq *ContactDefaultQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cdq.driver.Dialect())
	t1 := builder.Table(contactdefault.Table)
	columns := cdq.ctx.Fields
	if len(columns) == 0 {
		columns = contactdefault.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cdq.sql != nil {
		selector = cdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cdq.ctx.Unique != nil && *cdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cdq.predicates {
		p(selector)
	}
	for _, p := range cdq.order {
		p(selector)
	}
	if offset := cdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ContactDefaultGroupBy is the group-by builder for ContactDefault entities.
type ContactDefaultGroupBy struct {
	selector
	build *ContactDefaultQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cdgb *ContactDefaultGroupBy) Aggregate(fns ...AggregateFunc) *ContactDefaultGroupBy {
	cdgb.fns = append(cdgb.fns, fns...)
	return cdgb
}

// Scan applies the selector query and scans the result into the given value.
func (cdgb *ContactDefaultGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cdgb.build.ctx, "GroupBy")
	if err := cdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContactDefaultQuery, *ContactDefaultGroupBy](ctx, cdgb.build, cdgb, cdgb.build.inters, v)
}

func (cdgb *ContactDefaultGroupBy) sqlScan(ctx context.Context, root *ContactDefaultQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cdgb.fns))
	for _, fn := range cdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cdgb.flds)+len(cdgb.fns))
		for _, f := range *cdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ContactDefaultSelect is the builder for selecting fields of ContactDefault entities.
type ContactDefaultSelect struct {
	*ContactDefaultQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cds *ContactDefaultSelect) Aggregate(fns ...AggregateFunc) *ContactDefaultSelect {
	cds.fns = append(cds.fns, fns...)
	return cds
}

// Scan applies the selector query and scans the result into the given value.
func (cds *ContactDefaultSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cds.ctx, "Select")
	if err := cds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContactDefaultQuery, *ContactDefaultSelect](ctx, cds.ContactDefaultQuery, cds, cds.inters, v)
}

func (cds *ContactDefaultSelect) sqlScan(ctx context.Context, root *ContactDefaultQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cds.fns))
	for _, fn := range cds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
